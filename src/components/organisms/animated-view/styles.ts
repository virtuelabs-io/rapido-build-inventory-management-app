import { StyleSheet, Dimensions } from 'react-native';

const Styles = StyleSheet.create({
    image: { 
        width: "100%", 
        height: 150,
        borderRadius: 10
    }
})

export default Styles